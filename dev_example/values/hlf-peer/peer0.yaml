image:
  tag: 1.3.0

persistence:
  accessMode: ReadWriteOnce
  size: 1Gi

peer:
  databaseType: CouchDB
  couchdbInstance: cdb-peer0
  mspID: PeerMSP

secrets:
  peer:
    cert: hlf--peer0-idcert
    key: hlf--peer0-idkey
    caCert: hlf--peer-cacert
  channel: hlf--channel
  adminCert: hlf--peer-admincert
  adminKey: hlf--peer-adminkey
